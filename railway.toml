[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn app_mejorada:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --preload"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
